# PREREG · B y C con apoyo de A

## TRIADA paso 1 · qué aporta A a cada una

**A cerró tres cosas usables:** `Ω_acto` absoluto es la métrica portable (12,6 %) ·
`Ω_imp` = 0,983 ± 0,41 % es universal · y de `A > 1` sale el **primer umbral derivado**:
`Ω_acto < 0,983`.

### B · lo que A permite preguntar de otra forma

B se cerró en D-433 con `R ≥ 95 %`, y D-502 lo remidió al σ\* vigente: **falla, R = 0,0–78,0 %**.
Pero **la regla 175 dice que distinguibilidad y fidelidad son criterios distintos**, y D-504 ya
midió la mitad de distinguibilidad: **6/6 en tres tejidos**.

**B no se relaja: se PARTE en dos**, y cada mitad se reporta con su propio veredicto.

- **P-B1 · la mitad de distinguibilidad de B PASA en los tres tejidos** con el σ\* vigente.
- **P-B2 · la mitad de fidelidad FALLA** y su `R` tendrá una dispersión entre semillas ≫ que la
  de `Ω`, por el efecto escalón de la regla 183. Predigo: dispersión de `R` > 5× la de `Ω`.

### C · lo que A permite auditar

`TOL_conn` (ε = 4,4 %) alimenta **`L_tramo` (8,1 %) y `p_costura` (8,1 %)** — dos de las cuatro
cifras anchas. Y `TOL_conn` se midió **contra el contrato declarado**. Con el umbral derivado
`Ω < 0,983`, `TOL_conn` puede re-derivarse.

- **P-C1 · `TOL_conn` al umbral de DISTINGUIBILIDAD será ≥ 50× más flojo** que el 5,294e-3 actual.
- **P-C2 · pero eso NO cerrará `L_tramo` ni `p_costura`**, porque `TOL_conn` pesa sólo el 8,1 % de
  cada una; las dominan `FFN` (91,9 % y 45,9 %) y `L_neurita` (46 %). *Si acierta, A no puede
  mover C y hay que decirlo.*
- **P-C3 · la auditoría de las 10 cifras encontrará exactamente UNA que mide la máquina
  equivocada** (`Q_boquilla`, 368× fuera de precisión, D-492), y ninguna más.
