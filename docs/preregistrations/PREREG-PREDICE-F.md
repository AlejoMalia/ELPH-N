# PREREG · D+F y E+F · usar las leyes portables para PREDECIR F fuera de muestra

## La prueba más fuerte que queda

Ya hay dos leyes portables medidas:

- `Ω_D = suelo + c_D·Π^0,52` (D-510, ENTRE 3,2 %)
- `Ω_E = suelo + c_E·(σ*/esp)^2,1` (D-501, ENTRE 5,1 %)
- composición: unión sub-aditiva, razón **0,78** (D-424 y D-489, dos substratos)

**Si son leyes de verdad, deben PREDECIR un acto que no se ha medido.** Se elige una combinación
`(Π, σ*)` **nueva**, se predice `Ω_acto` en los tres espacios **antes** de correr, y se mide.

## El acto de prueba, elegido antes

`Π` = **1,5e-2** y `σ*/espaciado` = **0,30** — ninguna de las dos se ha corrido.

## Predicciones (se escriben los números concretos)

- **P-P1 · el error de predicción de `Ω_acto` será < 25 % en los tres espacios.**
- **P-P2 · el error será menor con la composición sub-aditiva (r = 0,78) que con la suma**, que
  es la comprobación de que la sub-aditividad es real y no un ajuste.
- **P-P3 · `A` predicha = √(0,983/Ω_pred) caerá dentro del 15 % de la medida.**
- **P-P4 · el orden de los tres espacios se predecirá correctamente.**

*Si P-P1 acierta, F deja de ser una capa que sólo mide: pasa a ser una capa que predice.*
